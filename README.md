# READ ME

⁕ Instructions for each exercise are provided in the `README file` inside each directory.

⁕ Uses JUnit 5 — make sure to **import** or **add the library** before running tests.

⁕ To check your code: run the test file (_UniversityTest.java_, _CelestialTest.java_, etc...)

⁕ Exception messages are checked exactly in tests — match them word-for-word.
